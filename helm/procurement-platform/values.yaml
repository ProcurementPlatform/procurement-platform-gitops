global:
  environment: base
  domain: ""
  awsRegion: us-east-1
  s3Bucket: ""
  kmsKeyId: ""
  # acmCertArn / wafAclArn are injected by CI/CD from Terraform's outputs at deploy time — never hand-typed.
  acmCertArn: ""
  wafAclArn: ""
  # Kgateway (Gateway API / Envoy) routing. One shared Gateway per environment
  # provisions a single Envoy proxy + NLB; each service attaches via its own
  # HTTPRoute. This replaces the previous per-service ALB Ingress objects.
  gateway:
    enabled: true
    className: kgateway
    nlb:
      scheme: internet-facing
      targetType: ip
  # Default-deny + explicit allow NetworkPolicies — see templates/networkpolicies.yaml
  networkPolicy:
    enabled: true

# Per-service defaults. Previously each charts/<service>/values.yaml carried these
# independently; consolidated here so every subchart's values come from this single
# umbrella file (plus values-dev.yaml/values-prod.yaml image+IRSA overrides on top).

identity-service:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000
  securityContext:
    allowPrivilegeEscalation: false
  service:
    type: ClusterIP
    port: 5001
    targetPort: 5001
  healthCheck:
    path: /api/health
  serviceMonitor:
    enabled: true
  # HTTPRoute attaches to the shared Kgateway Gateway. Gateway API ranks rules
  # by longest-prefix match automatically, so no group.order is needed.
  httproute:
    enabled: true
    pathPrefixes:
      - /api/auth
      - /api/users
      - /api/hr
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}

procurement-service:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000
  securityContext:
    allowPrivilegeEscalation: false
  service:
    type: ClusterIP
    port: 5003
    targetPort: 5003
  healthCheck:
    path: /api/health
  serviceMonitor:
    enabled: true
  httproute:
    enabled: true
    pathPrefixes:
      - /api/vendors
      - /api/purchase-requests
      - /api/purchase-orders
      - /api/contracts
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}

finance-service:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000
  securityContext:
    allowPrivilegeEscalation: false
  service:
    type: ClusterIP
    port: 5002
    targetPort: 5002
  healthCheck:
    path: /api/health
  serviceMonitor:
    enabled: true
  httproute:
    enabled: true
    pathPrefixes:
      - /api/invoices
      - /api/payments
      - /api/customers
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}

document-service:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000
  securityContext:
    allowPrivilegeEscalation: false
  service:
    type: ClusterIP
    port: 5004
    targetPort: 5004
  healthCheck:
    path: /api/health
  serviceMonitor:
    enabled: true
  httproute:
    enabled: true
    pathPrefixes:
      - /api/documents
      - /api/notifications
      - /api/audit
      # Gateway PathPrefix matching is segment-based — "/api/audit" does not
      # match "/api/audit-logs" (no "/" boundary). The frontend calls
      # /api/audit-logs (which 301-redirects to /api/audit server-side), so
      # both prefixes must be registered for that redirect to be reachable.
      - /api/audit-logs
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}

ai-service:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    fsGroup: 1000
  securityContext:
    allowPrivilegeEscalation: false
  service:
    type: ClusterIP
    port: 5006
    targetPort: 5006
  healthCheck:
    path: /api/health
  serviceMonitor:
    enabled: true
  httproute:
    enabled: true
    pathPrefixes:
      - /api/ai
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}

frontend:
  replicaCount: 1
  image:
    repository: ""
    pullPolicy: IfNotPresent
    tag: "latest"
  serviceAccount:
    create: true
    annotations: {}
    name: ""
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 101
    fsGroup: 101
  securityContext:
    allowPrivilegeEscalation: false
    # nginx listens on 8080 (unprivileged) now, so no NET_BIND_SERVICE needed —
    # drop ALL Linux capabilities (tightest posture).
    capabilities:
      drop:
        - ALL
  service:
    type: ClusterIP
    # Service still presented on :80 (HTTPRoute references frontend:80); it
    # forwards to the container's 8080 where nginx listens.
    port: 80
    targetPort: 8080
  healthCheck:
    path: /
  # Frontend is the catch-all (/). Longest-prefix ranking means the /api/*
  # routes above always win over this for API paths.
  httproute:
    enabled: true
    pathPrefixes:
      - /
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
  pdb:
    enabled: true
    minAvailable: 1
  extraEnv: {}
