apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: production
  labels:
    app: identity-service
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: identity-service
