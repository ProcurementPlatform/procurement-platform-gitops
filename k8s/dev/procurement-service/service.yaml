apiVersion: v1
kind: Service
metadata:
  name: procurement-service
  namespace: dev
  labels:
    app: procurement-service
spec:
  type: ClusterIP
  ports:
    - port: 5003
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: procurement-service
