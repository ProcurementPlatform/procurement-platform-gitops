apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: dev
  labels:
    app: document-service
spec:
  type: ClusterIP
  ports:
    - port: 5004
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: document-service
